var exercise = require('workshopper-exercise')();
var filecheck = require('workshopper-exercise/filecheck');
var execute = require('workshopper-exercise/execute');
var comparestdout = require('workshopper-exercise/comparestdout');
var fs = require('fs');
var path = require('path');
var chalk = require('chalk');




exercise = filecheck(exercise);
exercise = execute(exercise);

exercise.addProcessor(function(mode, cb) {
  var filename = path.join(process.cwd(), process.argv[3]);
  var users = [{ 'user': 'barney' }, { 'user': 'fred' }];

  fs.readFile(filename, 'utf-8', function(err, data) {
    try {
      eval(data);
    } catch(e) {
      console.log(chalk.red("Sorry you had a Syntax error! make sure all your quotes are closed"));
      console.log(chalk.yellow(e.message));
      exercise.emit('fail', 'Your syntax is a little off');
      // false is fail
      return cb(void 0, false);
    }

    if(typeof clone == 'undefined'){
      exercise.emit('fail', 'Make sure you defined clone properly, remember javascript is particular about spelling and capitilaztion');
      return cb(void 0, false);
    }
    var output = clone(users);
    if(mode === 'verify') {
      if(output[0].user !== users[0].user || output[0] !== output[0]) {

        exercise.emit('fail', 'Your clone function is off');
        // false is fail
        cb(void 0, false);
      }else {
        // true is pass
        cb(void 0, true);
      }
    }
  });
});

module.exports = exercise;
