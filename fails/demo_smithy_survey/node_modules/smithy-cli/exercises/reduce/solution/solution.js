// solution stuff here

function reduce(array, callback, start) {
	var begin = 0;
	var result = 0;

	if(start){
		begin = start;
	}

	for(var i = begin; i !== array.length; i++){
		result = callback(result, array[i]);
	}

	return result;
}